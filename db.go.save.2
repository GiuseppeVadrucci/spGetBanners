package main

import (
    "database/sql"
    "fmt"
    "log"
    
    _ "github.com/lib/pq"
    "github.com/jmoiron/sqlx"
)


-
-- Procedures
--

DELIMITER $$
CREATE PROCEDURE [cms].spGetBanners(@LanguageCode VARCHAR(30), @PageId INT, @DeviceTypeId TINYINT, @ErrorMessage VARCHAR(1000))
BEGIN
	SELECT * FROM banners where  BETWEEN min and max;
END $$
DELIMITER ;
}
